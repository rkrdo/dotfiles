export EDITOR=vim
export VIMCONFIG=~/.vim # TODO - check if needed
export VIMDATA=~/.vim # TODO - check if needed
export NNN_USE_EDITOR=1
# use ripgrep with fzf
export FZF_DEFAULT_COMMAND='rg --files --follow --hidden --no-ignore-vcs'